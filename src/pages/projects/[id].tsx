import React from 'react';
import { Button } from '~/components';

export default function ProjectPage() {
  return (
    <div>
      <h1>PROJECT PAGE PLACEHOLDER</h1>
      <Button onClick={() => alert('created a deck')}>Create Deck</Button>
    </div>
  );
}
